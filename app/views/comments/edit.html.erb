<h2>Edit your comment</h2>

<%= form_for(:comments, url: "/comments/#{@comment.id}", method: "PATCH") do |f| %>
  <%= f.label :body %>
  <%= f.text_area :body, :value => @comment.body %>
  <br />
  <br />
  <%= f.submit "Submit updates" %>
<% end %>
